interface:
  display_name: "Code Quality Check"
  short_description: "Review current changes for QA, security, and maintainability risks"
  default_prompt: "Use $code-quality-check to inspect current changes, run focused QA/security/code-quality passes, dedupe actionable findings, and return a prioritized verdict without implementing fixes."
