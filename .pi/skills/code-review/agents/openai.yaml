interface:
  display_name: "Code Review"
  short_description: "Review current changes for QA, security, and maintainability risks"
  default_prompt: "Use $code-review to inspect current changes, coordinate required QA/security/code-quality subagent passes, flag unapproved lint/typecheck bypasses, trace new invariants and caches to call sites that did not adopt them, apply CARDS architecture checks where design changed, dedupe actionable findings, and return a prioritized verdict without implementing fixes."
