interface:
  display_name: "Subagent Orchestrator"
  short_description: "Coordinate explicit delegated subagent work"
  default_prompt: "Use $subagent-orchestrator only when delegation was explicitly requested. Select scoped subagents, omit model overrides unless requested, keep direct skill-use in-session, coordinate outputs, and never simulate missing subagent results."
